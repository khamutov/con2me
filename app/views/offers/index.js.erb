$('#offers_content').append('<%= j render @offers %>');
<% if @offers.next_page %>
  $('.pagination').replaceWith('<%= j will_paginate @offers %>');
<% else %>
  $(window).off('scroll');
  $('.pagination').remove();
<% end %>