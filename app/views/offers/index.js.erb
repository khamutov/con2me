$('#offers_content').append('<%= j render @offers %>');
<% if @offers.next_page %>
  $('.pagination').replaceWith('<%= j paginate @offers, :params => { :dist => @dists.last } %>');
<% else %>
  $(window).off('scroll');
  $('.pagination').remove();
<% end %>